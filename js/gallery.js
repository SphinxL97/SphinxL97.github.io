async function loadJSON(u){return await (await fetch(u)).json()}let data=[];function card(x){let img=x.cover?`<img src="${x.cover}">`:"";return `<a class="beitie-card" href="${x.active?x.detail_url:'javascript:void(0)'}"><div class="cover">${img}</div><div class="card-body"><h3>${x.id} ${x.title}</h3><span class="tag">${x.script||'待补充'}</span><div class="status ${x.active?'ready':''}">${x.status}</div></div></a>`}function render(q=''){q=q.trim();document.getElementById('galleryGrid').innerHTML=data.filter(x=>!q||[x.id,x.title,x.script,x.creator,x.dynasty].join(' ').includes(q)).map(card).join('')}loadJSON('data/beitie_catalog.json').then(d=>{data=d;render();document.getElementById('gallerySearch').oninput=e=>render(e.target.value)})