
async function loadJSON(u){return await (await fetch(u)).json()}let data=[];function card(x){let img=x.cover?`<img src="${x.cover}" alt="${x.title}">`:'';return `<a class="beitie-card" href="${x.active?x.detail_url:'javascript:void(0)'}"><div class="cover">${img}</div><div class="card-body"><h3>${x.id} ${x.title}</h3><span class="tag">${x.script||'待补充'}</span><div class="status ${x.active?'ready':''}">${x.active?'可查看完整碑帖图片与详情':'封面入口，待补充详情'}</div></div></a>`}function render(q=''){q=q.trim();let arr=data.filter(x=>!q||[x.id,x.title,x.script,x.creator,x.dynasty,x.shelf_mark].join(' ').includes(q));document.getElementById('galleryGrid').innerHTML=arr.map(card).join('');let c=document.getElementById('countBox');if(c)c.textContent=arr.length}loadJSON('data/beitie_catalog.json').then(d=>{data=d;let q=new URLSearchParams(location.search).get('q')||'';let inp=document.getElementById('gallerySearch');inp.value=q;render(q);inp.oninput=e=>render(e.target.value)})
